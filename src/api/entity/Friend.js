export default class Friend {
  constructor (friend) {
    this.id = friend.id
    this.name = friend.name
    this.nickName = friend.nick_name
    this.owner = friend.owner
    this.avatar = friend.avatar
  }
}
